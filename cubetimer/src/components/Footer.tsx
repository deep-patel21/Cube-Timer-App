'use client';
import React, { useState } from 'react';

const Hero = () => {
  
  return (
    <div className='bg-green-500 mt-0 w-full p-5 text-center'>
           put all the hrefs to resources here
    </div>
  );
};

export default Hero;